local devicons = require("nvim-web-devicons")
local p = require("theme/palette")

devicons.set_icon({
    proto = {
        icon = "﬘",
        color = p.purple_400,
        name = "Proto",
    },
})
